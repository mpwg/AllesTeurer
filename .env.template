# AllesTeurer KMP - Environment Configuration Template
# Copy this file to .env and fill in your actual values
# Then run: gh secret set -f .env to push to GitHub repository secrets
#
# VARIABLE USAGE GUIDE:
# ===================
# ✅ Required for CI/CD builds (GitHub Actions)
# 🏠 Optional for local development only
# 🚀 Required for app store deployments only
#
# ALL variables in this file should be configured as GitHub repository secrets
# for CI/CD to work properly. Use: gh secret set -f .env

# =============================================================================
# APPLE DEVELOPER ACCOUNT (✅ Required for CI/CD iOS builds)
# =============================================================================
APPLE_ID=your-apple-id@example.com
DEVELOPMENT_TEAM=YOUR_DEVELOPMENT_TEAM_ID
APPSTORE_TEAM_ID=YOUR_APPSTORE_TEAM_ID

# =============================================================================
# APP STORE CONNECT API AUTHENTICATION (✅ Required for CI/CD iOS builds)
# =============================================================================
# Option 1: API Key (Recommended - more secure than session cookie)
APP_STORE_CONNECT_API_KEY_KEY_ID=your_api_key_id
APP_STORE_CONNECT_API_KEY_ISSUER_ID=your_issuer_id
# For enhanced security, you can store the private key content as base64-encoded:
# To encode your private key (PEM file) to base64, run: base64 -w 0 /path/to/AuthKey_XXXXXX.p8
# The system will automatically decode base64 content when detected
APP_STORE_CONNECT_API_KEY_CONTENT="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_CONTENT_HERE\n-----END PRIVATE KEY-----"
# Alternative base64 format (if you prefer):
# APP_STORE_CONNECT_API_KEY_CONTENT="BASE64_ENCODED_PRIVATE_KEY_CONTENT_HERE"

# Option 2: Session Cookie (Alternative - expires regularly)
# FASTLANE_SESSION=your_fastlane_session_cookie

# =============================================================================
# iOS CODE SIGNING (✅ Required for CI/CD iOS builds)
# =============================================================================
MATCH_PASSWORD=your_match_repository_password
MATCH_GIT_URL=https://github.com/your-org/certificates-repo.git
MATCH_GIT_BASIC_AUTHORIZATION=your_git_basic_auth_token

# =============================================================================
# ANDROID CONFIGURATION (🚀 Required for Play Store deployments only)
# =============================================================================
# Path to your Google Play Console service account JSON key
# Only needed if you want to upload to Play Store automatically
GOOGLE_PLAY_JSON_KEY_PATH=path/to/your-service-account-key.json

# =============================================================================
# ANDROID CODE SIGNING (✅ Required for CI/CD Android builds)
# =============================================================================
# Required for CI/CD Android builds
ANDROID_KEYSTORE_PASSWORD=your-keystore-password
ANDROID_KEY_ALIAS=your-key-alias
ANDROID_KEY_PASSWORD=your-key-password

# Alternative: configure these in your gradle.properties file for local development
# ANDROID_KEYSTORE_PATH=path/to/your/keystore.jks

# =============================================================================
# CI/CD ENVIRONMENT (✅ Required for GitHub Actions)
# =============================================================================
# Required for GitHub Actions keychain setup
KEYCHAIN_PASSWORD=your_ci_keychain_password

# Set to true when running in CI environment
# CI=true

# CI/CD environment settings (uncomment for CI)
# FASTLANE_DISABLE_COLORS=1

# =============================================================================
# FASTLANE CONFIGURATION (✅ Required for CI/CD builds)
# =============================================================================
# Reduce prompts and noise
FASTLANE_SKIP_UPDATE_CHECK=1
FASTLANE_HIDE_GITHUB_ISSUES=1
FASTLANE_HIDE_CHANGELOG=1
FASTLANE_DISABLE_COLORS=1
FASTLANE_OPT_OUT_USAGE=1
